spring:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  datasource:
    url: jdbc:postgresql://${POSTGRES_IP}:4201/epicalendar
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}